import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // For Docker/standalone deploys, add `output: "standalone"` and run
  // `node .next/standalone/server.js` (see README → Deployment).
};

export default nextConfig;
