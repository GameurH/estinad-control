# ESTINAD Control — environment
# Copy to .env.local and fill in. NEVER commit .env.local.

# Public — safe for the browser (auth + realtime only)
NEXT_PUBLIC_SUPABASE_URL=https://zhfietudqhbjuqjqfvpa.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only — used by server components / server actions after an
# authenticated platform-admin check. NEVER prefixed NEXT_PUBLIC_.
SUPABASE_SERVICE_ROLE_KEY=

# Deployment origin (used for metadata / redirects)
NEXT_PUBLIC_SITE_URL=https://control.estinad.com
